#include "sys.h"
#include "cas.h"
#include "spectrumcas.h"
#include "debug.h"

int spectrum_casRead(tape_t *tape, char *file) {
  return -1;
}

int spectrum_casInput(tape_t *tape, uint32_t eventCount, uint32_t clock) {
  return 0;
}
